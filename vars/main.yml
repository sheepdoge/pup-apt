---
keyservers_to_add: "{{ pup_apt_vars_keyservers_to_add }}"
apt_repositories_to_add: "{{ pup_apt_vars_apt_repositories_to_add }}"
packages_to_install: "{{ pup_apt_vars_packages_to_install }}"
debs_to_install: "{{ pup_apt_vars_debs_to_install }}"
packages_to_uninstall: "{{ pup_apt_vars_packages_to_uninstall }}"
